# Reference YOLO dataset spec (the actual data.yaml is generated by pseudo_label_sam.py
# into <yolo_dataset>/data.yaml; this documents the schema).
path: C:/Users/frank/batterycv-data/yolo   # dataset root
train: images/train                         # SAM pseudo-labeled frames
val: C:/Users/frank/batterycv-data/eval/images   # hand-verified eval frames (GT)
names:
  0: battery
