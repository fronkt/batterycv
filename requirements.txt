# Local analysis / inference env (CPU). GPU training env is built by vast/setup.sh.
numpy>=1.26,<3
pandas>=2.0
opencv-python>=4.9
pillow>=10.0
matplotlib>=3.8
pyyaml>=6.0
tqdm>=4.66
ultralytics>=8.3.0        # YOLO11 detector + ByteTrack (pulls CPU torch locally)
supervision>=0.22         # box/track visualization & dataset utils
# SAM pseudo-labeling runs on the GPU box; install there via vast/setup.sh:
#   segment-anything @ git+https://github.com/facebookresearch/segment-anything.git
